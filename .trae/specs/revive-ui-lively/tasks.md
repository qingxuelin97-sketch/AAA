# Tasks

- [# Tasks

- [ ] Task 1: 新建 `app-lively# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757`# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757` 调整为 `#ff8a6b`，`--accent-2` 调整为 `#f56b8e# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757` 调整为 `#ff8a6b`，`--accent-2` 调整为 `#f56b8e`，新增 `--lively-grad:# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757` 调整为 `#ff8a6b`，`--accent-2` 调整为 `#f56b8e`，新增 `--lively-grad: linear-gradient(135deg, #ff8a6b, #f56b8e)`
  - [ ] SubTask 2.2: 在 `body` 与 `.app-root`# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757` 调整为 `#ff8a6b`，`--accent-2` 调整为 `#f56b8e`，新增 `--lively-grad: linear-gradient(135deg, #ff8a6b, #f56b8e)`
  - [ ] SubTask 2.2: 在 `body` 与 `.app-root` 叠加极轻 SVG turbulence 噪点纹理（opacity 0.025，内# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757` 调整为 `#ff8a6b`，`--accent-2` 调整为 `#f56b8e`，新增 `--lively-grad: linear-gradient(135deg, #ff8a6b, #f56b8e)`
  - [ ] SubTask 2.2: 在 `body` 与 `.app-root` 叠加极轻 SVG turbulence 噪点纹理（opacity 0.025，内联 data-uri，不增加网络请求）
  - [ ] SubTask 2.3: 为关键卡片（hero/`.ah-hero`/钱包 hero）引入非对称圆角，# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757` 调整为 `#ff8a6b`，`--accent-2` 调整为 `#f56b8e`，新增 `--lively-grad: linear-gradient(135deg, #ff8a6b, #f56b8e)`
  - [ ] SubTask 2.2: 在 `body` 与 `.app-root` 叠加极轻 SVG turbulence 噪点纹理（opacity 0.025，内联 data-uri，不增加网络请求）
  - [ ] SubTask 2.3: 为关键卡片（hero/`.ah-hero`/钱包 hero）引入非对称圆角，打破规整 4px 网格

# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757` 调整为 `#ff8a6b`，`--accent-2` 调整为 `#f56b8e`，新增 `--lively-grad: linear-gradient(135deg, #ff8a6b, #f56b8e)`
  - [ ] SubTask 2.2: 在 `body` 与 `.app-root` 叠加极轻 SVG turbulence 噪点纹理（opacity 0.025，内联 data-uri，不增加网络请求）
  - [ ] SubTask 2.3: 为关键卡片（hero/`.ah-hero`/钱包 hero）引入非对称圆角，打破规整 4px 网格

- [ ] Task 3: 氛围背景浮动光斑
  - [ ] SubTask 3.1: 在 `body::before` / `.app-root::before` 加# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757` 调整为 `#ff8a6b`，`--accent-2` 调整为 `#f56b8e`，新增 `--lively-grad: linear-gradient(135deg, #ff8a6b, #f56b8e)`
  - [ ] SubTask 2.2: 在 `body` 与 `.app-root` 叠加极轻 SVG turbulence 噪点纹理（opacity 0.025，内联 data-uri，不增加网络请求）
  - [ ] SubTask 2.3: 为关键卡片（hero/`.ah-hero`/钱包 hero）引入非对称圆角，打破规整 4px 网格

- [ ] Task 3: 氛围背景浮动光斑
  - [ ] SubTask 3.1: 在 `body::before` / `.app-root::before` 加 2-3 个柔焦光斑（radial-gradient），分别以 18s/# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757` 调整为 `#ff8a6b`，`--accent-2` 调整为 `#f56b8e`，新增 `--lively-grad: linear-gradient(135deg, #ff8a6b, #f56b8e)`
  - [ ] SubTask 2.2: 在 `body` 与 `.app-root` 叠加极轻 SVG turbulence 噪点纹理（opacity 0.025，内联 data-uri，不增加网络请求）
  - [ ] SubTask 2.3: 为关键卡片（hero/`.ah-hero`/钱包 hero）引入非对称圆角，打破规整 4px 网格

- [ ] Task 3: 氛围背景浮动光斑
  - [ ] SubTask 3.1: 在 `body::before` / `.app-root::before` 加 2-3 个柔焦光斑（radial-gradient），分别以 18s/22s/28s 周期 transform 漂移
  - [ ] SubTask 3.2: 定义 `@keyframes# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757` 调整为 `#ff8a6b`，`--accent-2` 调整为 `#f56b8e`，新增 `--lively-grad: linear-gradient(135deg, #ff8a6b, #f56b8e)`
  - [ ] SubTask 2.2: 在 `body` 与 `.app-root` 叠加极轻 SVG turbulence 噪点纹理（opacity 0.025，内联 data-uri，不增加网络请求）
  - [ ] SubTask 2.3: 为关键卡片（hero/`.ah-hero`/钱包 hero）引入非对称圆角，打破规整 4px 网格

- [ ] Task 3: 氛围背景浮动光斑
  - [ ] SubTask 3.1: 在 `body::before` / `.app-root::before` 加 2-3 个柔焦光斑（radial-gradient），分别以 18s/22s/28s 周期 transform 漂移
  - [ ] SubTask 3.2: 定义 `@keyframes livelyDrift1/2/3`，仅# Tasks

- [ ] Task 1: 新建 `app-lively.css` 灵动层骨架并接入 `styles.css`
  - [ ] SubTask 1.1: 创建 `client/src/styles/app-lively.css`，写入文件头注释（说明本层为灵动焕新层、级联末位、可整体回滚）
  - [ ] SubTask 1.2: 在 `client/src/styles/app-motion.css` 之后追加 `@import './styles/app-lively.css';` 到 `styles.css` 末尾
  - [ ] SubTask 1.3: 定义本层 design tokens（`--lively-1/2/3`、`--lively-grad`、`--lively-spring`），覆盖 `--accent` 为新色相但保留 `--text/--muted` 不变

- [ ] Task 2: 去AI感品牌色相 + 手作质感
  - [ ] SubTask 2.1: 把 `--accent` 由 `#d97757` 调整为 `#ff8a6b`，`--accent-2` 调整为 `#f56b8e`，新增 `--lively-grad: linear-gradient(135deg, #ff8a6b, #f56b8e)`
  - [ ] SubTask 2.2: 在 `body` 与 `.app-root` 叠加极轻 SVG turbulence 噪点纹理（opacity 0.025，内联 data-uri，不增加网络请求）
  - [ ] SubTask 2.3: 为关键卡片（hero/`.ah-hero`/钱包 hero）引入非对称圆角，打破规整 4px 网格

- [ ] Task 3: 氛围背景浮动光斑
  - [ ] SubTask 3.1: 在 `body::before` / `.app-root::before` 加 2-3 个柔焦光斑（radial-gradient），分别以 18s/22s/28s 周期 transform 漂移
  - [ ] SubTask 3.2: 定义 `@keyframes livelyDrift1/2/3`，仅 transform/opacity
  - [ ] SubTask 3.3: 加挡位门控