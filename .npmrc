# 这些 scope 固定走官方源：npmmirror 会零星漏同步个别版本的 tarball
# （实例：@fontsource/noto-serif-sc@5.2.9 在镜像上 404，导致服务器部署失败），
# 而 scoped registry 的优先级高于 --registry 参数，镜像装其余包不受影响。
# @rolldown 的平台二进制同样常缺，一并固定（vite 8 构建依赖）。
@fontsource:registry=https://registry.npmjs.org/
@fontsource-variable:registry=https://registry.npmjs.org/
@rolldown:registry=https://registry.npmjs.org/
