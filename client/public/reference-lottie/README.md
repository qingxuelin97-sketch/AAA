# Selected APK Lottie assets

These selected first-party animation files were copied verbatim from the
authorized APK for migration into AAA. Their APK paths and SHA-256 values are
recorded in `docs/app-reference/apk-provenance.json`.

Brand artwork, logos, marketing assets, and vendor SDK resources are outside
the migration allowlist.
