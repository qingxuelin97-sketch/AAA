# 复制此文件为 client/.env 并填入你的后端地址，用于打包手机 App（Capacitor 原生壳）。
#
# 说明：
# - Web 同源部署（前端和后端同域 / 同反代）时留空即可，/api 走相对路径。
# - 打包成手机 App 后，webview 不在你的域下，必须填后端完整地址（裸 origin，不带路径）。
# - 正式版（npm run app:android）要求 https:// 开头；内测无域名场景可用
#   HTTP 调试版（npm run app:android:http），允许 http:// 地址（不能是 localhost）。
# - 改完 .env 后需重新构建（app:android:http / app:build）才会生效。
#
# 内测服务器示例：
VITE_API_BASE=http://120.27.249.73:4000

# 正式 HTTPS 版才需要：Google Cloud Console 中启用 Play Integrity API 的
# 数字项目编号（不是项目 ID）。该值不是密钥；构建脚本会写入插件配置。
# PLAY_INTEGRITY_CLOUD_PROJECT_NUMBER=
