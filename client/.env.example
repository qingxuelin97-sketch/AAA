# 复制此文件为 .env 并填入你的后端地址，用于打包手机 App（Capacitor 原生壳）。
#
# 说明：
# - Web 同源部署（前端和后端同域 / 同反代）时留空即可，/api 走相对路径。
# - 打包成手机 App 后，webview 运行在 https://localhost，相对路径 /api 会
#   请求到 localhost 本地而非你的服务器，因此必须填后端的完整 HTTPS 地址。
# - 地址必须以 https:// 开头（Android 9+ 默认禁明文 HTTP）。
#
# 示例：
#   VITE_API_BASE=https://api.your-domain.com
#   VITE_API_BASE=https://your-domain.com   （如果前后端同域、由反代分流 /api）
#
# 注意：改完 .env 后需重新 npm run build:app 才会生效。
VITE_API_BASE=
