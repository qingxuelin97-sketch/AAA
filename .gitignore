node_modules/
.npm-cache/
client/dist/
server/data.sqlite
server/data.sqlite-shm
server/data.sqlite-wal
server/uploads/*
!server/uploads/.gitkeep
shots/
shots-static/
mobile-shots/
server/mobile-test.tmp.sqlite*
.DS_Store
*.log
server/smoke.tmp.sqlite*
server/s7test.tmp.sqlite*
server/logstest.tmp.sqlite*
server/log-archives/
server/.jwt-secret
.env
# 安卓原生工程 —— 本地生成，不进仓库，避免 pull/push 冲突
android/
!.github/native/android/
!.github/native/android/*.java
ios/
*.apk
*.aab
*.ipa
# 签名密钥永不进仓库。android/ 已整体忽略，这里再兜一层通配：
# 密钥一旦泄露不可撤销——用它签过的包无法与冒名包区分，只能换 applicationId 重来。
*.keystore
*.jks
server/sec-test.tmp.sqlite*
server/sec-test.interceptor.mjs
server/diagnostic.tmp.sqlite*
client/*-smoke.png
client/appdiff.tmp/
client/cssaudit.tmp/
server/wb-vars-test.tmp.sqlite*
server/regen-test.tmp.sqlite*
server/regen-test.interceptor.mjs
server/ctx-test.tmp.sqlite*

# 设计画布：只忽略产物，源文件要入库。
# 产物内含画布编辑器约 2.5MB，可用 /design 的 seed-canvas.mjs 从 *.dc.html 重新生成。
# 注意顺序：先忽略全部 .html，再把 .dc.html 源文件放回来 —— 反过来写不生效。
.design-canvas/*.html
!.design-canvas/*.dc.html
